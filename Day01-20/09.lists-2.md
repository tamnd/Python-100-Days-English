## Common Data Structures: Lists, Part 2

### List Methods

List variables have many methods that help you manipulate them. If we have a list named `foos` and it has a method named `bar`, we call it with `foos.bar()`. This is the standard object-method call syntax. Later, when we cover object-oriented programming, we will discuss it in more detail.

#### Adding and Removing Elements

A list is a **mutable container**, which means we can add elements to it, remove elements from it, and modify existing elements.

We can append an element with `append` and insert one at a specific position with `insert`.

```python
languages = ['Python', 'Java', 'C++']
languages.append('JavaScript')
print(languages)  # ['Python', 'Java', 'C++', 'JavaScript']
languages.insert(1, 'SQL')
print(languages)  # ['Python', 'SQL', 'Java', 'C++', 'JavaScript']
```

We can remove a specific element with `remove`. If the element is not present, Python raises `ValueError`, so it is a good idea to check first with `in`.

We can also remove elements with `pop`. By default, `pop` removes the last element, but it can also remove an element at a given index. If the index is out of range, Python raises `IndexError`. Finally, `clear` removes all elements from a list.

```python
languages = ['Python', 'SQL', 'Java', 'C++', 'JavaScript']
if 'Java' in languages:
    languages.remove('Java')
if 'Swift' in languages:
    languages.remove('Swift')
print(languages)  # ['Python', 'SQL', 'C++', 'JavaScript']
languages.pop()
temp = languages.pop(1)
print(temp)       # SQL
languages.append(temp)
print(languages)  # ['Python', 'C++', 'SQL']
languages.clear()
print(languages)  # []
```

> **Note**: `pop` returns the removed element. In the example above, the result is stored in `temp`, and then appended back later.

If a list contains multiple `'Python'` values, `languages.remove('Python')` only removes the first one.

Another way to remove an element is with the `del` keyword:

```python
items = ['Python', 'Java', 'C++']
del items[1]
print(items)  # ['Python', 'C++']
```

#### Finding Positions and Counts

The `index` method returns the index of an element. If the element is missing, it raises `ValueError`. The `count` method returns how many times an element appears.

```python
items = ['Python', 'Java', 'Java', 'C++', 'Kotlin', 'Python']
print(items.index('Python'))     # 0
print(items.index('Python', 1))  # 5
print(items.count('Python'))     # 2
print(items.count('Kotlin'))     # 1
print(items.count('Swift'))      # 0
# print(items.index('Java', 3))  # ValueError: 'Java' is not in list
```

#### Sorting and Reversing

The `sort` method sorts a list in place, and `reverse` reverses it.

```python
items = ['Python', 'Java', 'C++', 'Kotlin', 'Swift']
items.sort()
print(items)  # ['C++', 'Java', 'Kotlin', 'Python', 'Swift']
items.reverse()
print(items)  # ['Swift', 'Python', 'Kotlin', 'Java', 'C++']
```

### List Comprehensions

Python can also create lists with a special syntax called a **list comprehension**. The benefits are easiest to see through examples.

Scenario 1: create a list of numbers from `1` to `99` that are divisible by `3` or `5`.

```python
items = []
for i in range(1, 100):
    if i % 3 == 0 or i % 5 == 0:
        items.append(i)
print(items)
```

Using a list comprehension:

```python
items = [i for i in range(1, 100) if i % 3 == 0 or i % 5 == 0]
print(items)
```

Scenario 2: given a list of integers `nums1`, create a new list `nums2` containing their squares.

```python
nums1 = [35, 12, 97, 64, 55]
nums2 = []
for num in nums1:
    nums2.append(num ** 2)
print(nums2)
```

Using a list comprehension:

```python
nums1 = [35, 12, 97, 64, 55]
nums2 = [num ** 2 for num in nums1]
print(nums2)
```

Scenario 3: given a list of integers `nums1`, create a new list `nums2` containing only the elements greater than `50`.

```python
nums1 = [35, 12, 97, 64, 55]
nums2 = []
for num in nums1:
    if num > 50:
        nums2.append(num)
print(nums2)
```

Using a list comprehension:

```python
nums1 = [35, 12, 97, 64, 55]
nums2 = [num for num in nums1 if num > 50]
print(nums2)
```

List comprehensions are not only shorter and cleaner, they are often faster than building a list with `for` and `append`. As a rule of thumb, **strongly prefer list comprehensions when creating lists**.

### Nested Lists

Python does not require all list elements to have the same data type. A list can even contain other lists. When list elements are themselves lists, we call the result a **nested list**. Nested lists can represent tables or matrices.

For example, to store the scores of 5 students in 3 courses:

```python
scores = [[95, 83, 92], [80, 75, 82], [92, 97, 90], [80, 78, 69], [65, 66, 89]]
print(scores[0])
print(scores[0][1])
```

If you want to enter those scores from the keyboard:

```python
scores = []
for _ in range(5):
    temp = []
    for _ in range(3):
        score = int(input('Enter score: '))
        temp.append(score)
    scores.append(temp)
print(scores)
```

If you want to generate them randomly:

```python
import random

scores = [[random.randrange(60, 101) for _ in range(3)] for _ in range(5)]
print(scores)
```

> **Note**: The inner list comprehension creates one list of 3 random scores. The outer comprehension repeats that 5 times, producing a nested list.

### A Practical List Example

Let us use a lottery-number generator to demonstrate list usage. In the Chinese "Double Color Ball" lottery, each ticket contains `6` red balls selected from `1` to `33`, and `1` blue ball selected from `1` to `16`.

<img src="res/day09/lottery.png" style="zoom:85%;">

Here is one way to generate a random ticket:

```python
"""
Random number generator for Double Color Ball

Author: Luo Hao
Version: 1.0
"""
import random

red_balls = list(range(1, 34))
selected_balls = []
# Add 6 red balls to the selected list.
for _ in range(6):
    index = random.randrange(len(red_balls))
    selected_balls.append(red_balls.pop(index))
# Sort the selected red balls.
selected_balls.sort()
# Print the red balls.
for ball in selected_balls:
    print(f'\033[031m{ball:0>2d}\033[0m', end=' ')
# Randomly choose 1 blue ball.
blue_ball = random.randrange(1, 17)
# Print the blue ball.
print(f'\033[034m{blue_ball:0>2d}\033[0m')
```

> **Note**: The control sequences like `\033[031m` are used to print colored output in the terminal.

We can simplify this with `random.sample` and `random.choice`:

```python
"""
Random number generator for Double Color Ball

Author: Luo Hao
Version: 1.1
"""
import random

red_balls = [i for i in range(1, 34)]
blue_balls = [i for i in range(1, 17)]
selected_balls = random.sample(red_balls, 6)
selected_balls.sort()
for ball in selected_balls:
    print(f'\033[031m{ball:0>2d}\033[0m', end=' ')
blue_ball = random.choice(blue_balls)
print(f'\033[034m{blue_ball:0>2d}\033[0m')
```

If you want to generate `N` tickets, place the same logic inside a loop:

```python
"""
Random number generator for Double Color Ball

Author: Luo Hao
Version: 1.2
"""
import random

n = int(input('How many tickets to generate: '))
red_balls = [i for i in range(1, 34)]
blue_balls = [i for i in range(1, 17)]
for _ in range(n):
    selected_balls = random.sample(red_balls, 6)
    selected_balls.sort()
    for ball in selected_balls:
        print(f'\033[031m{ball:0>2d}\033[0m', end=' ')
    blue_ball = random.choice(blue_balls)
    print(f'\033[034m{blue_ball:0>2d}\033[0m')
```
