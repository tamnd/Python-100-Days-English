## Common Data Structures: Tuples

In the previous two lessons, we studied lists, which are container types that let us store multiple values and process them in batches with loops. Python also has other container types. In this lesson, we introduce the **tuple**.

### Defining Tuples and Basic Operations

In Python, a tuple is also a sequence of elements arranged in a specific order. The key difference from a list is that **a tuple is immutable**. Once a tuple is created, you cannot add or remove elements, and you cannot modify existing ones. Attempting to do so raises `TypeError`.

Tuples are usually defined with literal syntax like `(x, y, z)`.

```python
# A tuple with three elements.
t1 = (35, 12, 98)
# A tuple with four elements.
t2 = ('Luo Hao', 45, True, 'Chengdu, Sichuan')

print(type(t1))  # <class 'tuple'>
print(type(t2))  # <class 'tuple'>

print(len(t1))  # 3
print(len(t2))  # 4

print(t1[0])    # 35
print(t1[2])    # 98
print(t2[-1])   # Chengdu, Sichuan

print(t2[:2])   # ('Luo Hao', 45)
print(t2[::3])  # ('Luo Hao', 'Chengdu, Sichuan')

for elem in t1:
    print(elem)

print(12 in t1)            # True
print(99 in t1)            # False
print('Hao' not in t2)     # True

t3 = t1 + t2
print(t3)

print(t1 == t3)            # False
print(t1 >= t3)            # False
print(t1 <= (35, 11, 99))  # False
```

If a tuple has two elements, we call it a 2-tuple. If it has five elements, we call it a 5-tuple, and so on.

Be careful with single-element tuples. `()` represents the empty tuple, but if a tuple has exactly one element, you must include a trailing comma. Otherwise, Python treats the parentheses as grouping rather than tuple syntax.

```python
a = ()
print(type(a))  # <class 'tuple'>
b = ('hello')
print(type(b))  # <class 'str'>
c = (100)
print(type(c))  # <class 'int'>
d = ('hello', )
print(type(d))  # <class 'tuple'>
e = (100, )
print(type(e))  # <class 'tuple'>
```

### Packing and Unpacking

When you assign multiple comma-separated values to a variable, Python packs them into a tuple. When you assign a tuple to multiple variables, Python unpacks it.

```python
# Packing
a = 1, 10, 100
print(type(a))  # <class 'tuple'>
print(a)        # (1, 10, 100)

# Unpacking
i, j, k = a
print(i, j, k)  # 1 10 100
```

If the number of values does not match the number of variables, Python raises `ValueError`.

```python
a = 1, 10, 100, 1000
# i, j, k = a          # ValueError: too many values to unpack
# i, j, k, l, m, n = a # ValueError: not enough values to unpack
```

One solution is to use a starred expression so one variable can collect multiple values:

```python
a = 1, 10, 100, 1000
i, j, *k = a
print(i, j, k)        # 1 10 [100, 1000]
i, *j, k = a
print(i, j, k)        # 1 [10, 100] 1000
*i, j, k = a
print(i, j, k)        # [1, 10] 100 1000
*i, j = a
print(i, j)           # [1, 10, 100] 1000
i, *j = a
print(i, j)           # 1 [10, 100, 1000]
i, j, k, *l = a
print(i, j, k, l)     # 1 10 100 [1000]
i, j, k, l, *m = a
print(i, j, k, l, m)  # 1 10 100 1000 []
```

Two important notes:

- A starred target becomes a list, not a tuple.
- Only one starred expression can appear in one unpacking statement.

Unpacking works with all sequences, not just tuples. That includes lists, ranges, and strings.

```python
a, b, *c = range(1, 10)
print(a, b, c)
a, b, c = [1, 10, 100]
print(a, b, c)
a, *b, c = 'hello'
print(a, b, c)
```

### Swapping Variables

Swapping variable values is common in programming. In many languages, you need a temporary variable. In Python, swapping is simple:

```python
a, b = b, a
```

Likewise, rotating three values is also easy:

```python
a, b, c = b, c, a
```

### Tuples vs Lists

Why do we need tuples if Python already has lists? The short answer is that tuples and lists serve different purposes.

1. Tuples are immutable, which makes them better suited to multi-threaded environments because they reduce synchronization overhead.
2. Tuples are immutable, and in many cases **immutable types are faster to create** than their mutable equivalents.

We can use the `timeit` module to compare the creation cost of a tuple and a list containing the same elements:

```python
import timeit

print('%.3f seconds' % timeit.timeit('[1, 2, 3, 4, 5, 6, 7, 8, 9]', number=10000000))
print('%.3f seconds' % timeit.timeit('(1, 2, 3, 4, 5, 6, 7, 8, 9)', number=10000000))
```

Sample output:

```text
0.635 seconds
0.078 seconds
```

The exact results vary by hardware and software, but tuple creation is often noticeably faster.

Of course, tuples and lists can be converted into each other:

```python
infos = ('Luo Hao', 45, True, 'Chengdu, Sichuan')
print(list(infos))  # ['Luo Hao', 45, True, 'Chengdu, Sichuan']

frts = ['apple', 'banana', 'orange']
print(tuple(frts))  # ('apple', 'banana', 'orange')
```

### Summary

**Lists and tuples are both container types**, meaning a single variable can hold multiple values, and both are ordered sequences. **Lists are mutable**, while **tuples are immutable**. That is why lists support adding, deleting, clearing, sorting, and reversing elements, while tuples do not. Both support concatenation, membership testing, indexing, and slicing. We **recommend using list comprehensions to create lists**, because they are expressive and efficient.
